# ==============================================================================
# NELO v3.1-HARDENED: CORE SYSTEM INITIAL PARAMETERS
# ==============================================================================

# [MECCANICA DELL'OBLIO - SPEC-003]
# Tempo limite assoluto dopo il quale la SHADOW_BUFFER e la PRIVATE_RAM vengono 
# azzerate tramite wipe hardware TRNG, garantendo l'effimerità totale dei dati.
DECAY_INTERVAL_MS=120

# [FRIZIONE DIFENSIVA - DETEZIONE ANOMALIE]
# Latenza artificiale base iniettata per rallentare le risposte a livello hardware
# ogni volta che l'Indice di Rischio calcolato supera la soglia critica (RI > 0.7).
FRICTION_FACTOR_MS=500

# [TOLLERANZA BIZANTINA - SPEC-002]
# Numero minimo assoluto di firme valide richieste su un pool di estrazione fissa (N=127).
# Corrisponde alla formula crittografica blindata: floor(2/3 * 127) + 1.
QUORUM_REQUIREMENT_NODES=85
QUORUM_POOL_TOTAL=127
